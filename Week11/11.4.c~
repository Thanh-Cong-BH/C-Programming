#include <stdio.h>
void swap_3 (int *a, int *b, int *c)
{
  int tmp;
  tmp = *p;
  *p = *q;
  *q = *r;
  *r = tmp;
}

int main()
{
  int x,y,z;
  printf("Enter 3 integers: ");
  scanf("%d %d %d", &x, &y, &z);
  swap_3(&a,&b,&c);
  printf("Results: %d %d %d\n", x, y, z);
}
